# Type Definitions

This folder will contain type definitions for the application data structures.

Since we're using JavaScript for Phase 1, this will be used in future phases if we migrate to TypeScript.